package org.codefeedr.socketgenerator

case class SocketGeneratorConfig(
                                port: Int,
                                rate: Int,
                                totalEvents: Int
                                )
